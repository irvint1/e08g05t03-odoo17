# Upstream

This directory was sourced from the OCA Helpdesk repository and then vendored
into this deployment repo so the VM can get the addon code with a normal
`git pull`.

- Upstream repository: `https://github.com/OCA/helpdesk.git`
- Upstream branch used here: `18.0`
